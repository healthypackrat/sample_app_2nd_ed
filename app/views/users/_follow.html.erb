<%= form_with model: current_user.relationships.build(followed_id: @user.id), local: true do |f| %>
  <%= f.hidden_field :followed_id %>
  <%= f.submit 'Follow', class: 'btn btn-primary btn-block' %>
<% end %>
